#include <stdio.h>

int main(int argc, char *argv[])
{
    char *numero = argv[1]; //para introducir una promesa de argumentos
    printf("El numero que has introducido es %s \n", numero);
    return 0;
}